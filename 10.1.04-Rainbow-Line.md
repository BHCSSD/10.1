```
 10.1.4-Rainbow-Line
```




Sure, here's how you can divide your code into MVC without changing it. 

## Model
The model will contain the variables and their updates.

```javascript
// Model
let lineY = 50;

function updateLineY() {
    // for now we will leave this blank. 
}
```

## View
The view will contain the `setup` and `draw` functions, handling all the rendering.

```javascript
// View
function setup() {
    createCanvas(600, 600);
    background(255, 255, 0);
}// end of setup

function draw() {
    line(50, lineY, mouseX, mouseY);
    lineY = lineY + 2 // this really should not be in the View.. but meh, its ok for now.
}// end draw
```

## Controller
The controller will handle interactions and updating the model.

```javascript
// Controller
function mousePressed() {
    // No interactions defined yet
}// end of mouse pressed


```


